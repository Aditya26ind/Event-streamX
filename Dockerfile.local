# Local development image for EventStreamX
# This Dockerfile is used only by docker-compose for local testing.
FROM python:3.12-slim

WORKDIR /app
COPY . /app
RUN pip install -r requirements.txt
RUN mkdir -p /app/data

CMD ["python", "-m", "app.main"]
